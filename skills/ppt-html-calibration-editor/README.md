# ppt-html-calibration-editor

Optional local browser editor for fixed 1920×1080 HTML slides. It supports review-time text, geometry, typography, and image calibration while keeping source HTML as the production truth.

Use `?edit=1&capture=<slide-id>` for editing. Use `?capture=<slide-id>` for clean rendering; the editor must not mount in capture mode.
